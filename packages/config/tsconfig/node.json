{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Node / Electron main process (bundled by tsup)",
  "extends": "./base.json",
  "compilerOptions": {
    "lib": ["ES2022"],
    "types": ["node"],
    "noEmit": true
  }
}
