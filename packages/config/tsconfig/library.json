{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Library (just-in-time, consumed as TS source)",
  "extends": "./base.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node"]
  }
}
